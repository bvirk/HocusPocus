<?php
return ["<!<div class='auto80'>#html#</div>",actors\tocHeadline($func),<<<EOMD
mv means rename file - i.e. change name but limited to the same directory.  
Image path and path for js and css for the specific page in which the original file was data content, is changed too.
EOMD,actors\srclf('progs/NNNAPI.php','function mv',14),<<<EOMD
$srcExpl

Postfix WOE in variable name for 'without extension'.  
'|' is encoding of '.' in url.  
selname denotes the name of selected name in dir dirlist and txtinput the prompt that opens for some command, which here is mv.  
'path' is the current directory for the dirlist - it starts with pages/ and thus addresses from data/ in webdir and is also path in pages/ in webdir.
</div>

EOMD,actors\tocNavigate($func)];